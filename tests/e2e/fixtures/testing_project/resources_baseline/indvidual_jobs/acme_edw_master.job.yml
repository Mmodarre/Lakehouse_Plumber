# Generated by LakehousePlumber - Master Orchestration Job for acme_edw
# This job coordinates the execution of all individual jobs

resources:
  jobs:
    acme_edw_master:
      name: acme_edw_master
      max_concurrent_runs: 1
      tasks:
        - task_key: j_four_task
          depends_on:
            - task_key: j_three_task
          job_task:
            job_id: ${resources.jobs.j_four.id}
        - task_key: j_nine_task
          job_task:
            job_id: ${resources.jobs.j_nine.id}
        - task_key: j_one_task
          job_task:
            job_id: ${resources.jobs.j_one.id}
        - task_key: j_seven_task
          job_task:
            job_id: ${resources.jobs.j_seven.id}
        - task_key: j_six_task
          job_task:
            job_id: ${resources.jobs.j_six.id}
        - task_key: j_three_task
          depends_on:
            - task_key: j_two_task
          job_task:
            job_id: ${resources.jobs.j_three.id}
        - task_key: j_two_task
          depends_on:
            - task_key: j_one_task
          job_task:
            job_id: ${resources.jobs.j_two.id}
      
      # Master job configuration
      queue:
        enabled: false
      performance_target: STANDARD
      
      # You can add additional configuration like tags, notifications, etc.
      # tags:
      #   orchestration: "true"
      #   project: "acme_edw"

