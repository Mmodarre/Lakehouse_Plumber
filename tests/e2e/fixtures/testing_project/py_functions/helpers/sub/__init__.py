"""Nested sub-package for the helper-copying E2E fixture.

Proves the closure copies a nested sub-package with its directory structure
preserved (``helpers/sub/deep.py`` -> ``custom_python_functions/helpers/sub/deep.py``).
"""
