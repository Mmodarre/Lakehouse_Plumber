# UC tags for uc_customers — column tags only (no table-level tags), migrated out
# of schemas/uc_customers.yaml. Exercises an empty-string column tag value; the
# absent `tags:` key keeps the table out of _TABLE_TAGS (absent != empty).
version: 1.0.0
table: uc_customers
columns:
  - name: c_email
    tags:
      classification: pii
      masked: ''
