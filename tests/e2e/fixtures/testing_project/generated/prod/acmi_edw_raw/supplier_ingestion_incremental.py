# Generated by LakehousePlumber
# Pipeline: acmi_edw_raw
# FlowGroup: supplier_ingestion_incremental

from pyspark import pipelines as dp
from pyspark.sql import functions as F

# Pipeline Configuration
PIPELINE_ID = "acmi_edw_raw"
FLOWGROUP_ID = "supplier_ingestion_incremental"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.view()
def v_supplier_raw_cloudfiles():
    """Load supplier_raw Parquet files from landing volume"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "parquet")
        .option("cloudFiles.maxFilesPerTrigger", 50)
        .option("cloudFiles.inferColumnTypes", True)
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
        .option("cloudFiles.rescuedDataColumn", "_rescued_data")
        .load("/Volumes/acme_edw_prod/edw_raw/incoming_volume/supplier/*.parquet")
    )

    # Add operational metadata columns
    df = df.withColumn("_processing_timestamp", F.current_timestamp())
    df = df.withColumn("_source_file_path", F.col("_metadata.file_path"))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="acme_edw_prod.edw_raw.supplier_raw", comment="Streaming table: supplier_raw"
)


# Define append flow(s)
@dp.append_flow(
    target="acme_edw_prod.edw_raw.supplier_raw",
    name="f_supplier_raw_cloudfiles",
    comment="Append flow to acme_edw_prod.edw_raw.supplier_raw",
)
def f_supplier_raw_cloudfiles():
    """Append flow to acme_edw_prod.edw_raw.supplier_raw"""
    # Streaming flow
    df = spark.readStream.table("v_supplier_raw_cloudfiles")

    return df
