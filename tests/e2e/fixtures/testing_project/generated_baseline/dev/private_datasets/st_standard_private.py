# Generated by LakehousePlumber
# Pipeline: private_datasets
# FlowGroup: st_standard_private

from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "private_datasets"
FLOWGROUP_ID = "st_standard_private"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_private_orders_src():
    """Load source rows for the private standard streaming table"""
    df = spark.readStream.table("acme_edw_dev.edw_raw.customer_raw")

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="acme_edw_dev.edw_bronze.private_orders_st",
    comment="Private streaming table (standard mode); not published to metastore",
    private=True,
)


# Define append flow(s)
@dp.append_flow(
    target="acme_edw_dev.edw_bronze.private_orders_st",
    name="f_private_orders_st",
    comment="Write private standard-mode streaming table",
)
def f_private_orders_st():
    """Write private standard-mode streaming table"""
    # Streaming flow
    df = spark.readStream.table("v_private_orders_src")

    return df
