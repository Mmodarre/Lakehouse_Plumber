# Generated by LakehousePlumber
# Pipeline: private_datasets
# FlowGroup: mv_private

from pyspark.sql import DataFrame
from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "private_datasets"
FLOWGROUP_ID = "mv_private"


# ============================================================================
# TARGET TABLES
# ============================================================================


@dp.materialized_view(
    name="acme_edw_dev.edw_gold.private_customer_summary_mv",
    comment="Private materialized view: per-pipeline customer summary (not published to metastore)",
    table_properties={},
    private=True,
)
def private_customer_summary_mv():
    """Write private customer summary materialized view"""
    # Materialized views use batch processing
    df = spark.sql("""SELECT
  c_mktsegment AS market_segment,
  COUNT(*) AS customer_count
FROM acme_edw_dev.edw_raw.customer_raw
GROUP BY c_mktsegment""")

    return df
