# Generated by LakehousePlumber
# Pipeline: 17_delta_cdc
# FlowGroup: cdc_time_travel

from pyspark.sql import DataFrame
from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "17_delta_cdc"
FLOWGROUP_ID = "cdc_time_travel"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_customers_snapshot_v10():
    """Load customers as of Delta version 10"""
    df = spark.read.option("versionAsOf", "10").table(
        "acme_edw_dev.edw_silver.customers"
    )

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================


@dp.materialized_view(
    name="acme_edw_dev.edw_gold.customers_snapshot_v10",
    comment="Snapshot of customers at Delta version 10 (time-travel)",
    table_properties={},
)
def customers_snapshot_v10():
    """Write to acme_edw_dev.edw_gold.customers_snapshot_v10 from multiple sources"""
    # Materialized views use batch processing
    df = spark.read.table("v_customers_snapshot_v10")

    return df
