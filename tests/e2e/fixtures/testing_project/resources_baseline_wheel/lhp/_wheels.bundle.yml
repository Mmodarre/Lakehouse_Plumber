# Generated by LakehousePlumber - Wheel packaging bundle wiring
# Regenerated on every `lhp generate`. Do not edit.
#
# Points the bundle's artifact upload location at the UC volume and excludes the
# `_wheels/` staging dir from file sync. No `artifacts:` block: each wheel is a
# prebuilt local library reference in environment.dependencies, which DAB uploads
# to <artifact_path>/.internal/ and rewrites itself (declaring a whl artifact with
# no files would make DAB try to REBUILD the prebuilt wheel).
targets:
  dev:
    workspace:
      artifact_path: /Volumes/acme_edw_dev/edw_raw/artifacts

sync:
  exclude:
    - "../../generated/${bundle.target}/_wheels/**"