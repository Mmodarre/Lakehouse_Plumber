pipeline: perf_modelled
flowgroup: fan_club_modelled
presets:
  - default_delta_properties
actions:
  - name: fan_club_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: fan_club_dim
    target: v_fan_club_silver
    description: "Load fan_club from silver for modelling"

  - name: fan_club_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_fan_club_silver
    target: v_fan_club_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_fan_club_silver

  - name: write_fan_club_modelled
    type: write
    source: v_fan_club_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "fan_club_modelled"
