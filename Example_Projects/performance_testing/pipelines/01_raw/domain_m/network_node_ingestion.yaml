pipeline: domain_m_raw
flowgroup: network_node_ingestion
presets:
  - default_delta_properties
use_template: csv_ingestion_template
template_parameters:
  table_name: network_node_raw
  landing_folder: network_node
  schema_file: partsupp_schema
