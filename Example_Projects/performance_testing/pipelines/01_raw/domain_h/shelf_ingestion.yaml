pipeline: perf_raw
flowgroup: shelf_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_shelf_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/shelf/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_shelf_cloudfiles
    description: "Load shelf from landing volume"

  - name: write_shelf_raw
    type: write
    source: v_shelf_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "shelf_raw"
