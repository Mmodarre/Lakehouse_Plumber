pipeline: domain_n_raw
flowgroup: quote_ingestion
presets:
  - default_delta_properties
use_template: parquet_ingestion_template
template_parameters:
  table_name: quote_raw
  landing_folder: quote
  schema_file: orders_schema
