pipeline: domain_p_raw
flowgroup: genome_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_genome_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/genome/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_genome_cloudfiles
    description: "Load genome from landing volume"

  - name: write_genome_raw
    type: write
    source: v_genome_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "genome_raw"
