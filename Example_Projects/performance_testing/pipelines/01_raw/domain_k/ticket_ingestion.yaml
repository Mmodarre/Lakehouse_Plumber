pipeline: perf_raw
flowgroup: ticket_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_ticket_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/ticket/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_ticket_cloudfiles
    description: "Load ticket from landing volume"

  - name: write_ticket_raw
    type: write
    source: v_ticket_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "ticket_raw"
