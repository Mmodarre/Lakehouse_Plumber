pipeline: domain_k_raw
flowgroup: airport_ingestion
presets:
  - default_delta_properties
use_template: parquet_ingestion_template
template_parameters:
  table_name: airport_raw
  landing_folder: airport
  schema_file: orders_schema
