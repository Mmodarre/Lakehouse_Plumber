pipeline: perf_gold
flowgroup: driver_score_monthly_agg_mv
actions:
  - name: driver_score_monthly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('mon', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.driver_score_dim
        GROUP BY 1
    target: v_driver_score_monthly_agg

  - name: write_driver_score_monthly_agg_mv
    type: write
    source: v_driver_score_monthly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "driver_score_monthly_agg_mv"
