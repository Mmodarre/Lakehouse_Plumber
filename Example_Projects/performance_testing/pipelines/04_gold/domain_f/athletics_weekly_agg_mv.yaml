pipeline: domain_f_gold
flowgroup: athletics_weekly_agg_mv
actions:
  - name: athletics_weekly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('week', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.athletics_dim
        GROUP BY 1
    target: v_athletics_weekly_agg

  - name: write_athletics_weekly_agg_mv
    type: write
    source: v_athletics_weekly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "athletics_weekly_agg_mv"
