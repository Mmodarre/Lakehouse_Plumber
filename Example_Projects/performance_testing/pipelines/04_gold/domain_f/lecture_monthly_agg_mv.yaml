pipeline: perf_gold
flowgroup: lecture_monthly_agg_mv
actions:
  - name: lecture_monthly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('mon', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.lecture_dim
        GROUP BY 1
    target: v_lecture_monthly_agg

  - name: write_lecture_monthly_agg_mv
    type: write
    source: v_lecture_monthly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "lecture_monthly_agg_mv"
