pipeline: domain_f_gold
flowgroup: dormitory_monthly_agg_mv
actions:
  - name: dormitory_monthly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('mon', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.dormitory_dim
        GROUP BY 1
    target: v_dormitory_monthly_agg

  - name: write_dormitory_monthly_agg_mv
    type: write
    source: v_dormitory_monthly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "dormitory_monthly_agg_mv"
