pipeline: perf_gold
flowgroup: payload_data_quarterly_agg_mv
actions:
  - name: payload_data_quarterly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('quarter', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.payload_data_dim
        GROUP BY 1
    target: v_payload_data_quarterly_agg

  - name: write_payload_data_quarterly_agg_mv
    type: write
    source: v_payload_data_quarterly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "payload_data_quarterly_agg_mv"
