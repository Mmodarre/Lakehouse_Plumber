pipeline: perf_gold
flowgroup: fan_dashboard_mv
actions:
  - name: fan_dashboard_sql
    type: load
    source:
      type: sql
      sql: |
        SELECT
          *,
          current_timestamp() as _report_generated_at
        FROM {catalog}.{silver_schema}.fan_dim
    target: v_fan_dashboard

  - name: write_fan_dashboard_mv
    type: write
    source: v_fan_dashboard
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "fan_dashboard_mv"
