pipeline: perf_gold
flowgroup: net_metering_weekly_agg_mv
actions:
  - name: net_metering_weekly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('week', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.net_metering_dim
        GROUP BY 1
    target: v_net_metering_weekly_agg

  - name: write_net_metering_weekly_agg_mv
    type: write
    source: v_net_metering_weekly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "net_metering_weekly_agg_mv"
