pipeline: perf_gold
flowgroup: blend_quarterly_agg_mv
actions:
  - name: blend_quarterly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('quarter', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.blend_dim
        GROUP BY 1
    target: v_blend_quarterly_agg

  - name: write_blend_quarterly_agg_mv
    type: write
    source: v_blend_quarterly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "blend_quarterly_agg_mv"
