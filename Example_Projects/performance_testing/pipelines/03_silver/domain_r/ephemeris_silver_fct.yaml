pipeline: perf_silver
flowgroup: ephemeris_silver_fct
presets:
  - default_delta_properties
actions:
  - name: ephemeris_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: ephemeris
    target: v_ephemeris_bronze
    description: "Load ephemeris from bronze for silver layer"

  - name: write_ephemeris_silver
    type: write
    source: v_ephemeris_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "ephemeris_fct"
      mode: "cdc"
      cdc_config:
        keys: ["ephemeris_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]
