pipeline: perf_bronze
flowgroup: ticket_bronze
presets:
  - default_delta_properties
actions:
  - name: ticket_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: ticket_raw
    target: v_ticket_raw
    description: "Load ticket from raw schema"

  - name: ticket_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_ticket_raw
    target: v_ticket_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as ticket_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_ticket_raw
      WHERE 1=1

  - name: ticket_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_ticket_bronze_cleaned
    target: v_ticket_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to ticket"

  - name: write_ticket_bronze
    type: write
    source: v_ticket_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "ticket"
