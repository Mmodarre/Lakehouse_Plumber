pipeline: perf_bronze
flowgroup: lab_result_bronze
presets:
  - default_delta_properties
actions:
  - name: lab_result_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: lab_result_raw
    target: v_lab_result_raw
    description: "Load lab_result from raw schema"

  - name: lab_result_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_lab_result_raw
    target: v_lab_result_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as lab_result_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_lab_result_raw
      WHERE 1=1

  - name: lab_result_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_lab_result_bronze_cleaned
    target: v_lab_result_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to lab_result"

  - name: write_lab_result_bronze
    type: write
    source: v_lab_result_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "lab_result"
