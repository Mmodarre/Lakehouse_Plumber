pipeline: perf_bronze
flowgroup: loss_report_bronze
presets:
  - default_delta_properties
actions:
  - name: loss_report_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: loss_report_raw
    target: v_loss_report_raw
    description: "Load loss_report from raw schema"

  - name: loss_report_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_loss_report_raw
    target: v_loss_report_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as loss_report_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_loss_report_raw
      WHERE 1=1

  - name: loss_report_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_loss_report_bronze_cleaned
    target: v_loss_report_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to loss_report"

  - name: write_loss_report_bronze
    type: write
    source: v_loss_report_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "loss_report"
