# Generated by LakehousePlumber
# Pipeline: acmi_edw_raw_ingestions
# FlowGroup: lineitem_ingestion_incremental

from pyspark.sql import functions as F
import dlt

# Pipeline Configuration
PIPELINE_ID = "acmi_edw_raw_ingestions"
FLOWGROUP_ID = "lineitem_ingestion_incremental"

# ============================================================================
# SOURCE VIEWS
# ============================================================================

# Schema hints for lineitem_cloudfiles table
lineitem_cloudfiles_schema_hints = """
    l_orderkey BIGINT,
    l_partkey BIGINT,
    l_suppkey BIGINT,
    l_linenumber INT,
    l_quantity DECIMAL(18,2),
    l_extendedprice DECIMAL(18,2),
    l_discount DECIMAL(18,2),
    l_tax DECIMAL(18,2),
    l_returnflag STRING,
    l_linestatus STRING,
    l_shipdate DATE,
    l_commitdate DATE,
    l_receiptdate DATE,
    l_shipinstruct STRING,
    l_shipmode STRING,
    l_comment STRING
""".strip().replace("\n", " ")


@dlt.view()
def v_lineitem_raw_cloudfiles():
    """Load lineitem_raw JSON files from landing volume"""
    df = spark.readStream \
        .format("cloudFiles") \
        .option("cloudFiles.format", "json") \
        .option("cloudFiles.maxFilesPerTrigger", 50) \
        .option("cloudFiles.inferColumnTypes", True) \
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns") \
        .option("cloudFiles.rescuedDataColumn", "_rescued_data") \
        .option("cloudFiles.schemaHints", lineitem_cloudfiles_schema_hints) \
        .load("/Volumes/acmi_edw_dev/edw_raw/landing_volume/lineitem/*.json")


    # Add operational metadata columns
    df = df.withColumn('_processing_timestamp', F.current_timestamp())
    df = df.withColumn('_source_file_path', F.col('_metadata.file_path'))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dlt.create_streaming_table(
    name="acmi_edw_dev.edw_raw.lineitem_raw",
    comment="Streaming table: lineitem_raw",
    table_properties={"delta.enableRowTracking": "true"})


# Define append flow(s)
@dlt.append_flow(
    target="acmi_edw_dev.edw_raw.lineitem_raw",
    name="f_lineitem_raw_cloudfiles",
    comment="Append flow to acmi_edw_dev.edw_raw.lineitem_raw"
)
def f_lineitem_raw_cloudfiles():
    """Append flow to acmi_edw_dev.edw_raw.lineitem_raw"""
    # Streaming flow
    df = spark.readStream.table("v_lineitem_raw_cloudfiles")

    return df
