# Generated by LakehousePlumber - Bundle Resource for gold_load
resources:
  pipelines:
    gold_load_pipeline:
      name: gold_load_pipeline
      catalog: main
      schema: lhp_${bundle.target}
      
      libraries:
        - glob:
            include: ../../generated/gold_load/**
      
      root_path: ${workspace.file_path}/generated/gold_load/
      
      configuration:
        bundle.sourcePath: ${workspace.file_path}/generated 