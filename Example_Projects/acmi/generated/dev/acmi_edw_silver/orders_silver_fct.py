# Generated by LakehousePlumber
# Pipeline: acmi_edw_silver
# FlowGroup: orders_silver_fct

from pyspark.sql import functions as F
import dlt

# Pipeline Configuration
PIPELINE_ID = "acmi_edw_silver"
FLOWGROUP_ID = "orders_silver_fct"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dlt.view()
def v_orders_bronze():
    """Load orders table from silver schema"""
    df = spark.readStream.table("acme_edw_dev.edw_bronze.orders")

    # Add operational metadata columns
    df = df.withColumn("_processing_timestamp", F.current_timestamp())

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table for CDC
dlt.create_streaming_table(
    name="acme_edw_dev.edw_silver.orders_fct",
    comment="Streaming table: orders_fct",
    table_properties={"delta.enableRowTracking": "true"},
)

# CDC mode using auto_cdc
dlt.create_auto_cdc_flow(
    target="acme_edw_dev.edw_silver.orders_fct",
    source="v_orders_bronze",
    keys=["order_id"],
    sequence_by="last_modified_dt",
    stored_as_scd_type=1,
    ignore_null_updates=False,
)
