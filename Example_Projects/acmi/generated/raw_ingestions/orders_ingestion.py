# Generated by LakehousePlumber
# Pipeline: raw_ingestions
# FlowGroup: orders_ingestion

from pyspark.sql import functions as F
import dlt

# Pipeline Configuration
PIPELINE_ID = "raw_ingestions"
FLOWGROUP_ID = "orders_ingestion"

# ============================================================================
# SOURCE VIEWS
# ============================================================================

@dlt.view()
def v_orders_cloudfiles():
    """Load orders Parquet files from landing volume"""
    df = spark.readStream \
        .format("cloudFiles") \
        .option("cloudFiles.format", "parquet") \
        .option("cloudFiles.maxFilesPerTrigger", 50) \
        .option("cloudFiles.inferColumnTypes", True) \
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns") \
        .option("cloudFiles.rescuedDataColumn", "_rescued_data") \
        .load("/Volumes/acmi_edw_dev/edw_raw/landing_volume/orders/*.parquet")


    # Add operational metadata columns
    df = df.withColumn('_source_file_path', F.col('_metadata.file_path'))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dlt.create_streaming_table(
    name="acmi_edw_dev.edw_raw.orders",
    comment="Streaming table: orders")


# Define append flow(s)
@dlt.append_flow(
    target="acmi_edw_dev.edw_raw.orders",
    name="f_orders_cloudfiles",
    comment="Append flow to acmi_edw_dev.edw_raw.orders"
)
def f_orders_cloudfiles():
    """Append flow to acmi_edw_dev.edw_raw.orders"""
    # Streaming flow
    df = spark.readStream.table("v_orders_cloudfiles")

    return df
