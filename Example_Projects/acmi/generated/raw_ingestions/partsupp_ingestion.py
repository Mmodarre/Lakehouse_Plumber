# Generated by LakehousePlumber
# Pipeline: raw_ingestions
# FlowGroup: partsupp_ingestion

from pyspark.sql import functions as F
import dlt

# Pipeline Configuration
PIPELINE_ID = "raw_ingestions"
FLOWGROUP_ID = "partsupp_ingestion"

# ============================================================================
# SOURCE VIEWS
# ============================================================================

@dlt.view()
def v_partsupp_cloudfiles():
    """Load partsupp Parquet files from landing volume"""
    df = spark.readStream \
        .format("cloudFiles") \
        .option("cloudFiles.format", "parquet") \
        .option("cloudFiles.maxFilesPerTrigger", 50) \
        .option("cloudFiles.inferColumnTypes", True) \
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns") \
        .option("cloudFiles.rescuedDataColumn", "_rescued_data") \
        .load("/Volumes/acmi_edw_dev/edw_raw/landing_volume/partsupp/*.parquet")


    # Add operational metadata columns
    df = df.withColumn('_source_file_path', F.col('_metadata.file_path'))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dlt.create_streaming_table(
    name="acmi_edw_dev.edw_raw.partsupp",
    comment="Streaming table: partsupp")


# Define append flow(s)
@dlt.append_flow(
    target="acmi_edw_dev.edw_raw.partsupp",
    name="f_partsupp_cloudfiles",
    comment="Append flow to acmi_edw_dev.edw_raw.partsupp"
)
def f_partsupp_cloudfiles():
    """Append flow to acmi_edw_dev.edw_raw.partsupp"""
    # Streaming flow
    df = spark.readStream.table("v_partsupp_cloudfiles")

    return df
