# Generated by LakehousePlumber
# Pipeline: apac_bronze
# FlowGroup: apac_orders_ingest

from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "apac_bronze"
FLOWGROUP_ID = "apac_orders_ingest"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_orders_raw():
    """Load data from csv files at /Volumes/dev_catalog/landing/files/apac/orders/*.csv"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "csv")
        .load("/Volumes/dev_catalog/landing/files/apac/orders/*.csv")
    )

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="dev_catalog.bronze.apac_orders", comment="Streaming table: apac_orders"
)


# Define append flow(s)
@dp.append_flow(
    target="dev_catalog.bronze.apac_orders",
    name="f_orders",
    comment="Append flow to dev_catalog.bronze.apac_orders",
)
def f_orders():
    """Append flow to dev_catalog.bronze.apac_orders"""
    # Streaming flow
    df = spark.readStream.table("v_orders_raw")

    return df
