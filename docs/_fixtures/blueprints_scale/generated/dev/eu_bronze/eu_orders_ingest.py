# Generated by LakehousePlumber
# Pipeline: eu_bronze
# FlowGroup: eu_orders_ingest

from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "eu_bronze"
FLOWGROUP_ID = "eu_orders_ingest"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_orders_raw():
    """Load data from csv files at /Volumes/dev_catalog/landing/files/eu/orders/*.csv"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "csv")
        .load("/Volumes/dev_catalog/landing/files/eu/orders/*.csv")
    )

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="dev_catalog.bronze.eu_orders", comment="Streaming table: eu_orders"
)


# Define append flow(s)
@dp.append_flow(
    target="dev_catalog.bronze.eu_orders",
    name="f_orders",
    comment="Append flow to dev_catalog.bronze.eu_orders",
)
def f_orders():
    """Append flow to dev_catalog.bronze.eu_orders"""
    # Streaming flow
    df = spark.readStream.table("v_orders_raw")

    return df
