# Generated by LakehousePlumber
# Pipeline: us_bronze
# FlowGroup: us_orders_ingest

from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "us_bronze"
FLOWGROUP_ID = "us_orders_ingest"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_orders_raw():
    """Load data from csv files at /Volumes/dev_catalog/landing/files/us/orders/*.csv"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "csv")
        .load("/Volumes/dev_catalog/landing/files/us/orders/*.csv")
    )

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="dev_catalog.bronze.us_orders", comment="Streaming table: us_orders"
)


# Define append flow(s)
@dp.append_flow(
    target="dev_catalog.bronze.us_orders",
    name="f_orders",
    comment="Append flow to dev_catalog.bronze.us_orders",
)
def f_orders():
    """Append flow to dev_catalog.bronze.us_orders"""
    # Streaming flow
    df = spark.readStream.table("v_orders_raw")

    return df
