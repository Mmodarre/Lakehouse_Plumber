# Development environment tokens.
# Referenced from the blueprint with ${token} syntax (resolved per environment,
# NOT inside pipeline:/flowgroup: names — those take %{var} only).
dev:
  catalog: dev_catalog
  bronze_schema: bronze
  landing_path: /Volumes/dev_catalog/landing/files
