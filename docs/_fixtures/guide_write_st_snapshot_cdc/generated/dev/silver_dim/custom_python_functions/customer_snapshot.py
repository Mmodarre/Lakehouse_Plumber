# LHP-SOURCE: functions/customer_snapshot.py
# Generated by LakehousePlumber - DO NOT EDIT
# Changes will be overwritten on next generation

from typing import Optional, Tuple

from pyspark.sql import DataFrame


def next_customer_snapshot(
    latest_version: Optional[int],
    *,
    snapshot_table: str,
) -> Optional[Tuple[DataFrame, int]]:
    """Return the next customer snapshot and its version, or None when exhausted."""
    if latest_version is None:
        query = f"SELECT min(snapshot_id) AS v FROM {snapshot_table}"
    else:
        query = (
            f"SELECT min(snapshot_id) AS v FROM {snapshot_table} "
            f"WHERE snapshot_id > {latest_version}"
        )
    nxt = spark.sql(query).collect()[0].v
    if nxt is None:
        return None
    df = spark.sql(f"SELECT * FROM {snapshot_table} WHERE snapshot_id = {nxt}").drop(
        "snapshot_id"
    )
    return (df, int(nxt))
