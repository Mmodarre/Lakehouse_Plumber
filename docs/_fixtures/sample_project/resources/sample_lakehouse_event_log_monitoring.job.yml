# Generated by LakehousePlumber - Monitoring Job for sample_lakehouse_event_log_monitoring

resources:
  jobs:
    sample_lakehouse_event_log_monitoring_job:
      name: sample_lakehouse_event_log_monitoring_job
      max_concurrent_runs: 1
      tasks:
        - task_key: union_event_logs
          notebook_task:
            notebook_path: ${workspace.file_path}/monitoring/${bundle.target}/union_event_logs
            source: WORKSPACE
        - task_key: sample_lakehouse_event_log_monitoring_pipeline
          depends_on:
            - task_key: union_event_logs
          pipeline_task:
            pipeline_id: ${resources.pipelines.sample_lakehouse_event_log_monitoring_pipeline.id}
            full_refresh: false
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZED
      tags:
        managed_by: "lakehouse_plumber"
        environment: "${bundle.target}"
