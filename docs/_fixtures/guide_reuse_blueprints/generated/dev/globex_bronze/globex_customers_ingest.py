# Generated by LakehousePlumber
# Pipeline: globex_bronze
# FlowGroup: globex_customers_ingest

from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "globex_bronze"
FLOWGROUP_ID = "globex_customers_ingest"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_customers_raw():
    """Load data from csv files at /Volumes/dev_catalog/landing/files/globex/eu/customers/*.csv"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "csv")
        .load("/Volumes/dev_catalog/landing/files/globex/eu/customers/*.csv")
    )

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="dev_catalog.bronze.globex_eu_customers",
    comment="Streaming table: globex_eu_customers",
    partition_cols=["ingest_date"],
)


# Define append flow(s)
@dp.append_flow(
    target="dev_catalog.bronze.globex_eu_customers",
    name="f_customers",
    comment="Append flow to dev_catalog.bronze.globex_eu_customers",
)
def f_customers():
    """Append flow to dev_catalog.bronze.globex_eu_customers"""
    # Streaming flow
    df = spark.readStream.table("v_customers_raw")

    return df
