# Development environment tokens.
# Referenced from the blueprint with ${token} syntax: these resolve per
# environment, and are NOT allowed inside pipeline:/flowgroup: names.
dev:
  catalog: dev_catalog
  bronze_schema: bronze
  landing_path: /Volumes/dev_catalog/landing/files
