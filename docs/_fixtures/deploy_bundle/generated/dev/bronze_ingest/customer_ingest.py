# Generated by LakehousePlumber
# Pipeline: bronze_ingest
# FlowGroup: customer_ingest

from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "bronze_ingest"
FLOWGROUP_ID = "customer_ingest"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_customers_raw():
    """Load data from csv files at /Volumes/dev_catalog/landing/files/customers/*.csv"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "csv")
        .load("/Volumes/dev_catalog/landing/files/customers/*.csv")
    )

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="dev_catalog.bronze.customers", comment="Streaming table: customers"
)


# Define append flow(s)
@dp.append_flow(
    target="dev_catalog.bronze.customers",
    name="f_customers_bronze",
    comment="Append flow to dev_catalog.bronze.customers",
)
def f_customers_bronze():
    """Append flow to dev_catalog.bronze.customers"""
    # Streaming flow
    df = spark.readStream.table("v_customers_raw")

    return df
