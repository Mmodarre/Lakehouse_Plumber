# Generated by LakehousePlumber
# Pipeline: silver_orders
# FlowGroup: orders_daily

from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "silver_orders"
FLOWGROUP_ID = "orders_daily"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_orders():
    """Delta source: dev_catalog.bronze.orders"""
    df = spark.readStream.table("dev_catalog.bronze.orders")

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="dev_catalog.silver.orders_daily", comment="Streaming table: orders_daily"
)


# Define append flow(s)
@dp.append_flow(
    target="dev_catalog.silver.orders_daily",
    name="f_orders_daily",
    comment="Append flow to dev_catalog.silver.orders_daily",
)
def f_orders_daily():
    """Append flow to dev_catalog.silver.orders_daily"""
    # Streaming flow
    df = spark.readStream.table("v_orders")

    return df
