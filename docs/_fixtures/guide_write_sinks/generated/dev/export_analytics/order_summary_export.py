# Generated by LakehousePlumber
# Pipeline: export_analytics
# FlowGroup: order_summary_export

from pyspark.sql import functions as F
from pyspark import pipelines as dp

# Pipeline Configuration
PIPELINE_ID = "export_analytics"
FLOWGROUP_ID = "order_summary_export"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_order_summary():
    """Delta source: dev_catalog.gold.order_summary"""
    df = spark.readStream.table("dev_catalog.gold.order_summary")

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Delta sink to analytics_shared.reporting.order_summary

# Create Delta sink
dp.create_sink(
    name="order_summary_export",
    format="delta",
    options={"tableName": "analytics_shared.reporting.order_summary"},
)


# Define append flow(s)
@dp.append_flow(
    target="order_summary_export",
    name="f_order_summary_export_1",
    comment="Delta sink to analytics_shared.reporting.order_summary",
)
def f_order_summary_export_1():
    df = spark.readStream.table("v_order_summary")

    return df
