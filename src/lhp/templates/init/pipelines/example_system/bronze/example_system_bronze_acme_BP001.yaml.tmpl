# Example blueprint instance — instantiates BP001_example_system_bronze for
# the "acme" variant. Rename this file to drop the `.tmpl` suffix to enable.
#
# Naming convention (BP-3.11): <system>_<layer>_<variant>_<BPxxx>.yaml.
#
# use_blueprint: example_system_bronze
# parameters:
#   variant: acme
#   source_path: s3://example-bucket/acme/raw/
#   file_format: csv
